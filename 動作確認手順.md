# pair-canvas 動作確認のやりかた

各フェーズのzipを実際に動かして見た目を確認するための手順です。
上から順番にコピペしていけばOK。

---

## 0. 準備

各フェーズのzip(`pair-canvas-phase1-basics.zip` など)を、好きな場所に展開(解凍)してください。
複数フェーズを見比べたいときは、フェーズごとに別フォルダに展開しておくと分かりやすいです。

例：
```
G:\mydev\pair-canvas-phase1-basics
G:\mydev\pair-canvas-phase2-design
G:\mydev\pair-canvas-phase3-safari
```

---

## 1. PowerShellを開いて、展開したフォルダに移動する

Windowsキー→「PowerShell」と入力→Enter でPowerShellが開きます。

開いたら、展開したフォルダに移動(cd)します。フォルダのパスをそのままコピペすればOK
(ドライブが違っても、フルパスを書けば普通に移動できます)。

```powershell
cd G:\mydev\pair-canvas-phase1-basics
```

---

## 2. もし `npm` がエラーになったら

環境によっては、下記のようなエラーが出ることがあります。

```
npm : このシステムではスクリプトの実行が無効になっているため...
```

これはPowerShellのセキュリティ設定によるもので、壊れているわけではないです。
以下のどちらかで回避できます。

**その場しのぎでいいなら**：`npm` の代わりに `npm.cmd` と打つ

```powershell
npm.cmd install
```

**毎回やるのが面倒なら**：下記を1回だけ実行して設定を変える

```powershell
Set-ExecutionPolicy -Scope CurrentUser -ExecutionPolicy RemoteSigned
```
→ 聞かれたら `Y` でEnter。これ以降は普通に `npm` が使えるようになります。

---

## 3. 必要なファイルをインストールする

フォルダに移動できたら、まずこれを実行(初回だけでOK、少し時間がかかります)。

```powershell
npm install
```

---

## 4. 動かして見た目を確認する

```powershell
npm run dev
```

実行すると
```
Local:   http://localhost:5173/
```
のようなURLが表示されるので、それをブラウザ(Chromeなど)で開けば実際の画面が見れます。

- ブラウザの開発者ツール(キーボードの`F12`)→スマホアイコンをクリックすると、
  スマホ幅での見た目もその場で確認できます。
- 確認が終わったら、PowerShellの画面で `Ctrl + C` を押すと止まります。

---

## 5. (任意) 本番ビルドが通るか確認する

「実際にサイトとして公開できる状態か」を確認したいときだけ、下記も実行してみてください
(見た目確認だけなら手順4だけで十分です)。

```powershell
npm run build
```

エラーなく終われば、`dist` フォルダができます。それがそのままGitHub Pagesにアップロードする中身になります。

---

## 別のフェーズを試すとき

同じ手順を、別フォルダ(例: `pair-canvas-phase2-design`)に対してもう一度繰り返すだけです。
`cd` で移動するフォルダを変えて、`npm install` → `npm run dev` の順番。

---

## 見てほしいポイント(各フェーズ共通)

- PC幅・スマホ幅、両方でレイアウトが崩れていないか
- 画像を追加して、ドラッグで位置がずれる／スライダーで拡大縮小できるか
- 矢印の上下のラベルが、空欄のときはちゃんと消えているか
- (フェーズ2以降)サイドバーの見た目、関係性タブの「①②③＋」の並び
- (フェーズ3)PNG書き出しボタンを押したときに「書き出し中…」の表示が出て、ちゃんとPNGが保存されるか

気になる崩れや動きがあったら、そのままスクショ付きで教えてください。
