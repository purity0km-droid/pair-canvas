# pair-canvas 修正履歴

改修前の原本（`baseline-original` / コミット `938a149`）から、現在の `main` までに
何をどう変えたかの一覧です。

原本からの差分は **24ファイル・約 +3,000行 / −1,450行** です。

- 各変更の細かい理由は、コミットメッセージ本文とソース内の日本語コメントに書いてあります。
- 動かして確認する手順は `動作確認手順.md` を見てください。

---

## 全期間を通して守っていること

- **書き出されるPNGの絵柄は、原本の設計を維持しています。**
  名前タグ・関係性ラベル・プロット名の暗色スクリム（半透明の黒い下敷き）も、
  背景色や写真を自由に選べる仕様で文字の読みやすさを保つために必須なので、
  意図的に変えていません。
- **追加ライブラリはゼロ**です（無料・軽量方針）。
- 全フェーズで `npx eslint src` がクリーン、`npm run build` が成功することを確認済みです。

---

## フェーズ1：内部の整理と基本機能の追加

### 構造の整理

- 画面表示用（`Preview`）とPNG書き出し用（`ExportPreview`）に**同じレイアウト処理が
  二重に書かれていた**のを、`RelationSheet.jsx` 1つに統合しました。
  片方だけ直して見た目がズレる、ということが起きなくなりました。
- 使われていない `RelationEditor.jsx` / `RelationList.jsx` を削除。
- 共通処理を切り出し：
  - `utils/createRelation.js` … 関係性データの初期値と、古いJSONを読み込んだときの項目補完
  - `utils/relationLayout.js` … 件数からカードの大きさ（large/medium/small）を決める判定
  - `constants.js` … 関係性の最大件数、下書き保存のキー
- Lintエラーを解消。

### 表示の作りを変更（WYSIWYG化）

- 以前はスマホ幅のときにCSSがレイアウトそのものを組み替えていたため、
  **入力中に見えている配置と、実際に書き出されるPNGの配置が違う**ことがありました。
- 常に1000px幅の設計のまま、画面が狭いときは全体を丸ごと縮小表示する方式に変更。
  「入力中に見えている見た目 ＝ 書き出される見た目」になりました。

### 追加した機能

- **画像の位置・拡大の調整モーダル**。画像を選んだ直後に自動で開きます。
  実際のカードとまったく同じ仕組みで表示しているので、調整した位置がズレません。
- 画像アップローダーに、Xのアイコン設定風のクロップ枠を追加。
- **中央ラベルの2段目**（矢印の下。空欄なら表示されません。絵文字だけでもOK）。
- **左右のキャラクター設定をまとめて入れ替えるボタン**。
- 左右の**写真の大きさの比率**（均等／左を大きく／右を大きく）を「おまけ」機能として追加。
  普段は折りたたまれています。※この機能はフェーズ7で削除しました。
- **自動下書き保存**。タブを閉じたりリロードしても入力内容が残ります（この端末の中だけ。
  サーバーには何も送っていません）。
- フォント選択のプレビュー表示。

### 直した不具合

- ブラウザ標準の画像ドラッグと、位置調整のドラッグがぶつかっていた問題。
- 画像アップローダーを囲む `<label>` が、隠れているファイル選択ボタンへのクリックを
  横取りしていた問題。
- 初期表示のちらつき。
- サイドバーのクロップ枠と、実際のカードの見た目のズレ。

---

## フェーズ2：見た目のシームレス化

- サイドバーを**単色の紫（#6D5DFC）＋ボーダーレス**に刷新。
  白い箱と枠線で区切るのをやめ、余白と細い罫線1本だけで区切る構成にしました。
- キャラクター画像を**フレームレス化**（下端を紙の色へなじませる）。
- 関係性の番号タブに「＋」を統合（`①②③＋` が同じ丸ボタンで並ぶ形）。
- 左右入れ替えボタン・おまけ機能も、この配色に合わせて統一。

---

## フェーズ3：Safari での書き出しの安定化

PNG書き出しは、SafariやiPhoneで「文字化けする」「画像が消える」ことがある仕組みを
使っているため、その対策です。

- 以前の「とりあえず0.1秒待つ」という運任せの実装をやめ、
  **フォントの読み込み完了と、全画像のデコード完了を確認してから**書き出すようにしました。
- フォント埋め込み用のデータを1回だけ計算して使い回すようにして軽量化。
- 書き出し中はローディング表示に切り替え、ボタンを押せなくしました。
- Safari側の仕組み自体が不安定なため、保険としての「2回描画する」構造は残しています。

---

## フェーズ4：UIの整理と不具合修正

- **プレビューを表示専用にしました。**
  カードの中の画像を直接ドラッグして動かす機能を廃止（スクロール操作を拾って
  意図せず位置がずれるため）。位置調整はサイドバーのモーダルに一本化しました。
- 名前タグ・関係性ラベル・プロット名の形を、**ピル形（真円の端）から角丸四角に戻し**ました。
- **ヘッダーバーを新設。** 役割を分けました。
  - ヘッダー … ファイル操作（リセット／JSON読込・保存／PNG保存・高画質PNG）
  - 左カラム … 作品そのものの設定
  - **リセットボタンはこのとき新規に追加したものです**（自動保存があるため、
    まっさらに戻す手段がありませんでした。確認ダイアログ付き）。
- **左カラムを圧縮。** 見出し「ページ設定」を削除、フォントのサンプル欄を廃止、
  色見本を小さく、背景パターンを3列→5列、サイドバー幅 460px→400px。
- スクロールするのは左右のカラムの中だけにして、ページ全体は動かないようにしました。

### 直した不具合

- ページ全体がスクロールして左カラムが見切れる問題
  （画面外に隠したファイル選択ボタンが、サイドバーの枠を突き抜けていた）。
- 画像の置き場所をクリックしてもファイル選択が開かず、
  ドラッグ＆ドロップでしか画像を選べなかった問題。キーボード操作にも対応しました。

---

## フェーズ5：UIのフィードバック対応

- **フォントを原本に戻しました。**
  ヘッダーの「PairCanvas」と、左カラムの見出し（「関係性」「関係性1」）が
  明朝体になっていたのを、改修前と同じ表示に戻しています。
  ※ 書き出す絵の中で使うフォント（ページ設定で選ぶもの）は今までどおりです。
- **タイトルのチェックボックスのズレを修正。** 入力欄に対して7px上にずれていました。
- **①②③のタブで、編集ボックスそのものを切り替えるようにしました。**
  以前は関係性の件数ぶんの箱が縦に並んでいましたが、今は選んだ1つだけが表示されます。
- **背景色・文字色・フォントを1行にまとめました。** 左カラムがさらに1行ぶん詰まります。
- **フォント選択欄の「▼」が表示されていなかったのを修正**（フェーズ4から残っていた不具合）。
- **スマホ表示のヘッダーを、高さ固定の1行にしました。**
  以前は2段組みでヘッダーだけで画面の1/5近く（144px）を占めていたのを52pxに。
  ボタンも小さくし、狭い画面ではラベルを短縮表示（読込／保存／PNG／高画質／↺）。
  **スクロールしても画面上部に残る**ようにしました。
- **スマホのプレビューにズーム操作を追加しました。**
  以前は画面幅に合わせて約1/3に縮むため文字が読めませんでしたが、
  `− / 倍率 / ＋ / 全体表示` で100%〜300%まで拡大できます。
  拡大したぶんは指で左右になぞって見られます。
  ※ この操作は画面表示だけのもので、書き出されるPNGには影響しません。

### フェーズ5の追加対応

- **左カラムの文字サイズを揃えました。**
  「関係性」まわりに14px・12px・14pxが混在していたのを、
  見出し14px／入力欄13px／補助情報12pxの3段に整理。
  ※「捕食者 と 生贄」の行はCSSの指定が無く、サイドバーの基準サイズを
  そのまま継いでいたのが原因でした。
- **画像に重ねる名前と、関係性ラベルの文字サイズを揃えました。**
  矢印の下の段だけ15pxだったのを13pxに。
- **関係性ラベルは未入力なら表示しません。**
  上下とも入力があるときだけ出します。両方とも空なら中央には矢印だけが残ります。
  （以前は未入力のとき「関係性」という見本の文字がそのまま書き出されていました）
- **「左右を入れ替え」を「画像位置入替」に改称しました。**
  動作は変えていません（名前・補足情報もこれまでどおり一緒に入れ替わります）。
- **画像の下端のフェード（白く飛んで見える部分）を無効にしました。**
  プレビューと書き出しPNGの両方に反映されます。
- **背景色・文字色をカラーコードで指定できるようにしました。**
  ※この入力欄はフェーズ7で削除しました（ブラウザ標準のカラーピッカーに
  同じ機能があるため）。
- **スマホのプレビューの倍率がおかしくなる不具合を直しました。**
  拡大するとプレビューの列そのものが引き伸ばされ、さらに拡大される、という
  ループになっていました（375px幅で300%を指定すると実際は3.0倍になっていた）。
- **ズーム操作はスマホ幅のときだけ出すようにしました。**
  PCではウィンドウ幅に合わせた自動縮小のみで、大きさは触れません。
- **「サーバーへの保存・送信は行いません」の表記**をプレビューの下に追加しました。

---

## フェーズ6：画像の縮小とレイアウト切り替え

- **画像を枠より小さく縮小できるようにしました。**
  これまでは「枠が写真で埋まる大きさ」が最小で、そこから拡大することしか
  できませんでした。30%まで縮小できます（100%＝枠が写真で埋まる状態）。
  スライダーの名前を「拡大」→「大きさ」に変え、今の倍率を％で表示します。

- **レイアウトパターンを切り替えられるようにしました。**
  左カラムの「▼ レイアウト（おまけ）」の中で、6つから選べます。
  シート全体に効く設定で、関係性が1〜4件のどの場合でも使えます。

  | | 見せ方 |
  |---|---|
  | 対面 | 左右に並べ、中央に関係性（これまでの形・初期設定） |
  | 帯 | 写真をカード幅いっぱいに2分割し、継ぎ目に関係性を大きく出す |
  | 交差 | 2枚を重ねて「近さ」を絵にする |
  | 見出し | 関係性をカード上部の大見出しにする |
  | 双方向 | 矢印を上下2本に分け、左→右と右→左で別々の言葉を出す |
  | 語り | 説明文を主役にし、写真は丸く小さく添える |

  「双方向」は、これまでの「関係性（矢印の上）」と「関係性（矢印の下）」を
  そのまま →／← に割り当てています。新しい入力欄は増えていません。



---

## フェーズ7：フィードバック対応

- **画像を縮小したときに、切れていた部分が戻るようにしました。**
  これまでは「枠が埋まるように自動でトリミングされた絵」を100%としていたため、
  縮小しても切り落とされた部分は戻らず、切れた絵が小さくなるだけでした。
  今は、
  - 100% … これまでと同じ見た目（枠がぴったり埋まった状態）
  - 100%未満 … 縮小しながら切れていた部分が戻り、最終的に写真全体が枠に収まる
  - 100%超 … これまでどおり拡大してトリミング

  レイアウトを切り替えて枠の形が変わっても、正しく追従します。
  保存済みのデータは100%のときの見え方が変わりません。
  また、縮小の下限を30%→**20%**に下げました（写真と枠の形が大きく違うとき、
  30%では写真全体が収まりきらないことがあったため）。

- **画像の下の補足事項が中央寄りになっていたのを直しました。**
  画像の左端・右端にきっちり揃うようになります。
  （フェーズ6で入れてしまった不具合です）

- **背景色・文字色のカラーコード入力欄を削除しました。**
  ブラウザ標準のカラーピッカーに同じ機能があるためです。
  Chrome / Edge では、色を選ぶ画面の「R G B」表示の右にある切り替えボタンを
  押すとカラーコード（HEX）表示になります。

- **「写真の大きさ（おまけ）」機能を削除しました。**
  関係性ごとに左右の写真の大きさの比率を変える機能です。
  これにより左カラムの「レイアウト（おまけ）」は1つ（レイアウトの切り替え）
  だけになりました。
  ※この機能を使って保存した古いJSONも、そのまま読み込めます。

- **折りたたみの三角マークが2つ並んでいたのを1つにしました。**

---

## フェーズ8：フィードバック対応

- **カードの並び順を入れ替えられるようにしました。**
  関係性が2件以上あるとき、編集ボックスの「⇄ 画像位置入替」の下に
  **「カード位置 ◀前へ ／ 次へ▶」** が出ます。
  シート上のカードの位置がそのまま入れ替わり、「関係性1」「関係性2」…の
  番号も自動で振り直されます。動かした関係性は選ばれたままなので、
  そのまま続けて編集できます。

  ※「画像位置入替」は1枚のカードの中で左右を入れ替えるもの、
  「カード位置」はカードそのものの順番を入れ替えるもので、別の機能です。

- **「語り」レイアウトの補足情報を、名前のすぐ下へ移動しました。**
  補足情報は「その人の情報」なので、2人ぶんをひとまとめに見せる位置に
  変えています。並び順は
  丸い写真 → 名前 → 補足情報 → 関係性 → 説明文 → プロット名 になります。

- **「語り」レイアウトの丸い写真を大きくしました。**
  168 / 180 / 110px →  **230 / 240 / 140px**（関係性1件・2件・3〜4件のとき）。

- **「帯」レイアウトの中央の ⇄ を黒にしました。**
  写真の上に直接乗るため、暗い写真でも消えないよう白のにじみは残しています。

---

## フェーズ9：フィードバック対応

- **「語り」レイアウトの説明文の文字サイズを、大・中・小から選べるようにしました。**
  左カラムの「▼ レイアウト（おまけ）」で「語り」を選ぶと、そのすぐ下に
  「説明の文字サイズ」という3つのボタン（小 / 中 / 大）が出ます。
  これまでの見た目が「中」で、「小」は約0.85倍、「大」は1.25倍です。

  実際の文字の大きさは、関係性の件数によって変わります。

  | | 小 | 中（これまで） | 大 |
  |---|---|---|---|
  | 関係性1〜2件 | 12.75px | 15px | 18.75px |
  | 関係性3〜4件 | 10.2px | 12px | 15px |

  行間も文字サイズに合わせて自動で広がります。
  この設定はシート全体に効くもので、関係性ごとの設定ではありません。
  JSONに保存されるので、書き出し・読み込みでも引き継がれます。
  「語り」以外のレイアウトを選んでいるときはボタンは出ませんが、
  選んだ大きさは残っているので、また「語り」に戻せばそのまま使えます。

---

## フェーズ10：フィードバック対応（最新）

- **説明文の文字サイズを、スライダーで自由に決められるようにしました。**
  フェーズ9では「語り」限定の 小／中／大 の3段でしたが、
  **スライダーで 100%〜200% を5%刻み**で調整する形に作り直し、
  **どのレイアウトでも使える**ようにしました。
  左カラムの「▼ レイアウト（おまけ）」の中にあります。

  100%のときの文字の大きさは、レイアウトごとに違います。

  | | 100%のときの大きさ |
  |---|---|
  | 語り（関係性1〜2件） | 12.75px（フェーズ9の「小」と同じ） |
  | 語り（関係性3〜4件） | 10.2px（同上） |
  | 語り以外 | 12px（これまでと同じ） |

  「語り」だけ100%の基準が小さめなのは、「小を基準にして上へ伸ばしたい」
  というご指示によるものです。語り以外は**これまでの大きさがそのまま100%**なので、
  触らなければ見た目は変わりません。
  行間も文字サイズに合わせて自動で広がります。

  フェーズ9のJSONを読み込んだ場合は、そのときの見た目に近い倍率へ
  自動で読み替えます（小→100% / 中→120% / 大→145%）。

- **関係性ラベルの太字をやめ、他のレイアウトと揃えました。**
  「語り」「見出し」「帯」の3つだけ太字になっていたのを、
  他と同じ太さ（標準）にしています。
  文字の大きさは、それぞれのレイアウトの持ち味なのでそのままです
  （帯18px／見出し24px／語り15px。※関係性1〜2件のとき）。

- **「帯」レイアウトの中央の矢印を白基調に戻しました。**
  写真の上に直接乗るため、明るい写真でも消えないよう暗いにじみを添えています。

---

## まだ手をつけていないこと

1. **PNG書き出しの実機確認**（とくに Safari / iPhone）。
   フェーズ3で対策した箇所です。開発側の環境ではダウンロードの動作まで
   確認できないため、実機での確認をお願いします。
2. スマホ幅では左カラムが画面の高さの半分までの内部スクロールになっています
   （フェーズ2からの挙動）。全体を1本のスクロールにするかは未決定です。
3. フェーズ6のレイアウト切り替えについて、実際の立ち絵を入れたときの
   見え方は未確認です。とくに「帯」は写真の顔の位置によって中央の
   関係性ラベルと重なることがあり、「交差」は重なった側の写真の左端が
   隠れます。実データで一度確認をお願いします。
